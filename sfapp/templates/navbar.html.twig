<nav class="navbar navbar-expand-lg">
    <div class="container">
        <a class="navbar-brand" href="/">
            <ul class="navbar-nav ms-lg-5 me-lg-auto">
                <li class="nav-item">
                    <span class="nav-link click-scroll" >SMART CAMPUS</span>
                </li>
            </ul>
        </a>

        <div class="d-lg-none ms-auto me-4">
            <a href="#top" class="navbar-icon bi-person smoothscroll"></a>
        </div>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-lg-5 me-lg-auto">
                {% if is_granted('ROLE_TECHNICIEN') %}
                    <!-- Navbar pour les techniciens -->
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_guide') }}">Guide</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_technicien_acceuil') }}"><Accueil>Accueil</Accueil></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_technicien_commentaire') }}">Commentaires</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_salle_liste') }}">Salles</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_sa_liste') }}">Sa</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_plan_liste') }}">Plan</a>
                    </li>



                {% elseif is_granted('ROLE_CHARGE_DE_MISSION') %}
                    <!-- Navbar pour les chargés de mission -->
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_guide') }}">Guide</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_dashboard_salle') }}">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_technicien_commentaire') }}">Commentaires</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_gestion') }}">Gestion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_batiment_liste') }}">Batiment</a>
                    </li>
                {% else %}
                    <!-- Navbar pour les autres rôles -->
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="{{ path('app_salle_user_liste') }}">Salles</a>
                    </li>



                {% endif %}
            </ul>

            <div class="d-none d-lg-block">
                {% if app.user %}
                    <span class="navbar-text">Bienvenue, {{ app.user.username }}</span>
                    <a class="btn btn-primary btn-sm ms-2" href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a class="btn btn-primary btn-sm" href="{{ path('app_login') }}">Connexion</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
