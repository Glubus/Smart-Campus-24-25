{% extends 'base.html.twig' %}

{% block body %}
    <div style="display: flex; height: 100vh;">

        <!-- Barre de navigation à gauche -->
        <div style="width: 20%; background-color: #f8f9fa; padding: 20px; border-right: 1px solid #ddd;">
            <h3>Navigation</h3>
            <ul style="list-style-type: none; padding: 0;">
                <li style="margin-bottom: 10px;">
                    <a href="{{ path('app_guide') }}" style="text-decoration: none; color: #007bff;">Guide site</a>
                </li>
                <li>
                    <a href="{{ path('users_auth_ldap') }}" style="text-decoration: none; color: #007bff;">Guide SA</a>
                </li>
            </ul>
        </div>

        <!-- Contenu principal à droite -->
        <div style="flex-grow: 1; padding: 20px;">
            <h1>Bienvenue</h1>
            <p>Veuillez sélectionner une option à gauche pour afficher le contenu correspondant.</p>
        </div>

    </div>
{% endblock %}