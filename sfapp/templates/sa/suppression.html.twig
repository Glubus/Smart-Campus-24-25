{% extends 'base.html.twig' %}

{% block title %}Ajouter un systeme d'acquisition (SA){% endblock %}

{% block body %}
    <a class="btn btn-primary margin-10" href="{{ path('app_sa_liste')}}" role="button">Retour</a>
    <div class="container my-5">
        <h1 class="text-center mb-4">Ajouter un système d'acquisition (SA)</h1>
        <a class="btn btn-primary margin-10" href="{{ path('app_sa_ajout')}}" role="button">Ajouter un SA</a>
        <table class="table table-striped table-bordered text-center align-middle">
            <thead>
            <tr class="table-dark">
                <th class="nom">Nom</th>

                <th class="col"></th>
                <th>
                </th>
            </tr>
            </thead>
            <tbody>

        {% for SA in liste_SA %}
            <tr>
                <td class="nom">
                    {{ SA.nom }}
                </td>
                <td class="detail">
                    <a href="">
                        voir
                    </a>
                </td>
                <td>
                    <a href="" class="btn btn-primary">
                        Supprimer
                    </a>
                </td>

            </tr>
        {% endfor %}
    </div>
{% endblock %}
