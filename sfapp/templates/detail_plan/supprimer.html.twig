{% extends 'base.html.twig' %}

{% block title %}Supprimer une attribution {% endblock %}

{% block body %}
    <div class="container my-5" style="width: 40%">
        {% if detail_plan.etatSA == 'pret' %}
            <h1 class="text-center mb-4"> {{ 'Demande pour la deinstallation de ' ~ detail_plan.SA.getNom ~ ' dans ' ~ detail_plan.Salle.getNom ~ ' ?'}}</h1>
        {% else %}
            <h1 class="text-center mb-4"> {{ detail_plan.etatSA ~ ' de ' ~ detail_plan.SA.getNom ~ ' dans ' ~ detail_plan.Salle.getNom}}</h1>
            <p>Êtes vous sur de vouloir supprimer cette attribution ?</p>
        {% endif %}
        <form method="post">
            <a class="btn btn-primary" href="{{ path('app_lier_liste', {'id':detail_plan.plan.id}) }}" style="margin-right: 10px; margin-top: 10px">Annuler</a>
            <button class="btn btn-primary" style="margin-right: 10px; margin-top: 10px">Continue</button>
        </form>
    </div>
{% endblock %}
